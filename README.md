# Install

To install this fork, run the following:

```bash
brew install cmake
conda create -n rl_course
conda activate rl_course
conda install python=3.9
pip install torch torchvision torchaudio requests pillow
python build.py build
python build.py install
```

Reach out to me if you've got any installation issues
